<template>
  <img src="" alt="Server Image" />
</template>
<!-- <script setup lang="ts">
import { ref, defineProps, withDefaults, computed, watch } from "vue";
// import { Server, type ServerModel } from "@45drives/houston-common-lib";

const props = withDefaults(defineProps<{ server?: Server }>(), { server: () => new Server() });

// const serverModel = ref<ServerModel>();

// const image = computed(() => serverModel.value?.imageURLs.chassisOverview);

// watch(
//   () => props.server,
//   (server) => {
//     server.getServerModel().map((model) => {
//       serverModel.value = model;
//     });
//   },
//   { immediate: true }
// );
</script> -->
